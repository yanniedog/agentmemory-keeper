# Generated by install.ps1
launch-hidden.vbs
launch-watchdog.vbs
launch-watcher.vbs
watchdog.ps1

# User overrides (copy config.json -> config.local.json)
config.local.json

# Local logs / state if someone points stateDir/logDir at the repo
logs/
state.json
keeper-state.json
*.log
